### PHP সম্পর্কিত প্রশ্ন:
  ##### PHP কী এবং এটি কীভাবে কাজ করে?
-   PHP (Hypertext Preprocessor) একটি সার্ভার-সাইড স্ক্রিপ্টিং ভাষা যা ওয়েব ডেভেলপমেন্টে ব্যবহৃত হয়। এটি HTML কোডের সাথে মিশে সার্ভারে প্রোসেসিং করার পরে ক্লায়েন্টকে ফলাফল প্রদান করে।

##### PHP এবং HTML এর মধ্যে পার্থক্য কী?
- HTML হল একটি মার্কআপ ভাষা যা ওয়েব পেজের স্ট্রাকচার নির্ধারণ করে। PHP হল একটি সার্ভার-সাইড স্ক্রিপ্টিং ভাষা যা ডাইনামিক কন্টেন্ট তৈরি করতে সাহায্য করে।
-   PHP এর সর্বশেষ সংস্করণ কোনটি?

#####   PHP তে ভ্যারিয়েবল ডিক্লেয়ার করার নিয়ম কী?
- PHP তে ভ্যারিয়েবল ডলারের চিহ্ন ($) দিয়ে শুরু হয়। যেমন: $variable = "value";

#####   PHP তে কিভাবে কোড কমেন্ট করা হয়?
- একক লাইন কমেন্ট: // comment
- মাল্টি-লাইন কমেন্ট: /* comment */
- 
#####   PHP তে $GLOBALS কী এবং এটি কিভাবে ব্যবহার করা হয়?
-$GLOBALS অ্যারে PHP স্ক্রিপ্টের সমস্ত গ্লোবাল ভ্যারিয়েবল ধারণ করে। এটি গ্লোবাল ভ্যারিয়েবল অ্যাক্সেস করার জন্য ব্যবহার করা হয়।

#####   PHP তে include এবং require এর মধ্যে পার্থক্য কী?
- include: ফাইল অন্তর্ভুক্ত করতে ব্যবহৃত হয়, কিন্তু ফাইল না পাওয়া গেলে সতর্কীকরণ দেখায়।
- require: ফাইল অন্তর্ভুক্ত করতে ব্যবহৃত হয়, কিন্তু ফাইল না পাওয়া গেলে ত্রুটি (fatal error) দেখায়
- 
#####   PHP তে $\_GET এবং $\_POST এর মধ্যে পার্থক্য কী?
- $_GET: URL মাধ্যমে ডেটা প্রেরণ করে, ডেটা URL এ দেখা যায়।
- $_POST: HTTP POST পদ্ধতির মাধ্যমে ডেটা প্রেরণ করে, ডেটা URL এ দেখা যায় না এবং এটি নিরাপদ।

#####    PHP তে session এবং cookie কীভাবে ব্যবহৃত হয়?
- session: সার্ভার সাইডে ব্যবহারকারীর তথ্য সংরক্ষণ করে এবং নিরাপত্তা প্রদান করে।
- cookie: ক্লায়েন্ট সাইডে তথ্য সংরক্ষণ করে এবং তথ্য সুরক্ষিত নাও হতে পারে।

#####  PHP তে একটি ফর্ম ডেটা সাবমিট করার পর সেটি কীভাবে প্রক্রিয়া করা হয়?
- ফর্ম ডেটা $_GET বা $_POST সপার্টিং সুপারগ্লোবাল অ্যারে দিয়ে প্রাপ্ত করা হয় এবং প্রক্রিয়া করা হয়।

#####  PHP তে mysqli এবং PDO এর মধ্যে পার্থক্য কী?
- mysqli: MySQL-specific, supports both procedural and object-oriented approaches.
- PDO: Database-agnostic, supports multiple databases, object-oriented only.

#####  PHP তে isset() এবং empty() ফাংশনের মধ্যে পার্থক্য কী?
- isset(): ভ্যারিয়েবল সেট করা আছে কি না চেক করে এবং ভ্যালু নাল নয় কিনা চেক করে।
- empty(): ভ্যারিয়েবলটি খালি (যেমন 0, "", NULL) কিনা চেক করে।


#####   PHP তে echo এবং print এর মধ্যে পার্থক্য কী?
- echo: একাধিক আর্গুমেন্ট গ্রহণ করতে পারে, দ্রুততর।
- print: একক আর্গুমেন্ট গ্রহণ করে এবং এটি একটি ভ্যালু রিটার্ন করে।


#####   PHP তে array কিভাবে তৈরি ও পরিচালনা করা হয়?
- Array তৈরি: $array = array(1, 2, 3);
- Array পরিচালনা: foreach, array_push(), array_pop(), ইত্যাদি ব্যবহার করা হয়।
- 

#####   PHP তে foreach লুপ কীভাবে কাজ করে?
- foreach লুপ array বা object এর প্রতিটি উপাদান iterate করতে ব্যবহৃত হয়।
````php
  foreach ($array as $value) {
    echo $value;
}
````
  
#####   PHP তে file_get_contents() এবং fopen() এর মধ্যে পার্থক্য কী?
- file_get_contents(): একটি সম্পূর্ণ ফাইলকে একটি স্ট্রিং হিসেবে পড়ে।
- fopen(): ফাইল খুলে এবং পড়া বা লেখা শুরু করতে ব্যবহার করা হয়, বেশি নিয়ন্ত্রণ প্রদান করে।


#####   PHP তে একটি এসকিউএল ইনজেকশন কী এবং এটি কিভাবে প্রতিরোধ করা যায়?
- QL Injection একটি আক্রমণ যা ম্যালিসিয়াস SQL কোড ডেটাবেসে প্রবাহিত হতে পারে। এটি প্রতিরোধ করতে প্যারামিটারাইজড কুয়েরি ও এসকেপিং ব্যবহার করা হয়।।


#####   PHP তে json_encode() এবং json_decode() ফাংশন কীভাবে কাজ করে?
- json_encode(): PHP অ্যারে বা অবজেক্টকে JSON স্ট্রিং এ রূপান্তর করে।
- json_decode(): JSON স্ট্রিং কে PHP অ্যারে বা অবজেক্টে রূপান্তর করে।


#####   PHP তে ডেটাবেস সংযোগ কিভাবে স্থাপন করবেন?
````php
$conn = new mysqli($host, $user, $password, $database);
````

#####   PHP তে try-catch ব্লক কী এবং কিভাবে এটি ব্যবহার করা হয়?
- try-catch ব্লক এক্সেপশন হ্যান্ডল করার জন্য ব্যবহৃত হয়।
````php
try {
    // Code that may throw an exception
} catch (Exception $e) {
    // Handle the exception
    echo 'Caught exception: ',  $e->getMessage(), "\n";
}
````

### OOP সম্পর্কিত প্রশ্ন:

#####   OOP এর চারটি মৌলিক ধারণা কী কী?
-   Encapsulation, Abstraction, Inheritance, Polymorphism।
  
  
#####   ক্লাস এবং অবজেক্টের মধ্যে পার্থক্য কী?
-   ক্লাস হল একটি ব্লুপ্রিন্ট যা অবজেক্ট তৈরি করার জন্য ব্যবহৃত হয়। অবজেক্ট হল ক্লাসের একটি ইনস্ট্যান্স।(instance)


#####  PHP তে constructor এবং destructor কী?
- constructor: একটি বিশেষ মেথড যা অবজেক্ট তৈরি করার সময় কল হয়।
- destructor: একটি বিশেষ মেথড যা অবজেক্ট ধ্বংস করার সময় কল হয়।


#####  ইনহেরিটেন্স কী এবং এটি কীভাবে কাজ করে?
- Inheritance একটি প্রক্রিয়া যার মাধ্যমে একটি ক্লাস (চাইল্ড ক্লাস) অন্য একটি ক্লাসের (প্যারেন্ট ক্লাস) বৈশিষ্ট্য এবং আচরণ লাভ করে।


#####   পলিমরফিজম কী এবং এটি কীভাবে বাস্তবায়ন করা হয়?
-   Polymorphism একই নামের মেথড বিভিন্ন কনটেক্সটে ভিন্নভাবে কাজ করে। এটি মেথড ওভাররাইডিংয়ের মাধ্যমে বাস্তবায়িত হয়।

   
#####    অ্যাবস্ট্রাকশন কী এবং এটি কীভাবে কাজ করে?
- Abstraction একটি প্রক্রিয়া যা শুধুমাত্র গুরুত্বপূর্ণ তথ্য প্রদান করে এবং অপ্রয়োজনীয় বিবরণ লুকিয়ে রাখে। এটি abstract ক্লাস এবং মেথড দিয়ে বাস্তবায়িত হয়।
  * publice
  * protected
  * private

#####   ইনক্যাপসুলেশন কী এবং এটি কীভাবে ব্যবহৃত হয়?
- Encapsulation হল ডেটা এবং মেথডগুলোকে একটি ক্লাসে বেঁধে রাখা এবং কিছু অংশকে অ্যাক্সেস কন্ট্রোল করার প্রক্রিয়া।

  
#####   PHP তে final কীওয়ার্ড কীভাবে ব্যবহৃত হয়?
-   final কীওয়ার্ড একটি ক্লাস বা মেথডকে ওভাররাইড করার জন্য নিষিদ্ধ করে।


#####  PHP তে static মেথড এবং প্রপার্টি কী?
-   static মেথড এবং প্রপার্টি ক্লাস লেভেলে সম্পর্কিত থাকে, অবজেক্ট লেভেলের নয়। এগুলি ক্লাসের নাম দিয়ে অ্যাক্সেস করা হয়।

  
#####    একটি ক্লাসের অবজেক্টের public এবং private প্রপার্টি গুলোকে কিভাবে এক্সেস করা হয়?
-   public প্রপার্টি সরাসরি অবজেক্ট থেকে অ্যাক্সেস করা যায়: $object->publicProperty
-   private প্রপার্টি শুধুমাত্র ক্লাসের মেথড দ্বারা অ্যাক্সেস করা যায় এবং বাইরের কোড থেকে সরাসরি অ্যাক্সেস করা সম্ভব নয়।

  
#####   PHP তে একটি ক্লাসের মধ্যে মেথড ওভারলোডিং কিভাবে করা যায়?
-   PHP তে প্রকৃত মেথড ওভারলোডিং সমর্থিত নয়। তবে, একটি মেথডের মধ্যে বিভিন্ন প্যারামিটার চেক করে আচরণ পরিবর্তন করা যেতে পারে।


#####   প্যারেন্ট ক্লাস এবং চাইল্ড ক্লাসের মধ্যে সম্পর্ক কিভাবে স্থাপন করা হয়?
-   extends কীওয়ার্ড ব্যবহার করে প্যারেন্ট ক্লাস থেকে চাইল্ড ক্লাস তৈরি করা হয়। উদাহরণ: class Child extends Parent {}

  
#####    PHP তে abstract ক্লাস এবং interface এর মধ্যে পার্থক্য কী?
- abstract ক্লাস: এক বা একাধিক অব্যবহৃত (abstract) মেথড থাকতে পারে, যা সাবক্লাস দ্বারা বাস্তবায়িত হয়।
- interface: শুধুমাত্র মেথডের সিগনেচার থাকে, কোন বাস্তবায়ন থাকে না। একাধিক ইন্টারফেস ব্যবহার করা যায়।

  
-   PHP তে ট্রেইট কী এবং এটি কিভাবে ব্যবহার করা হয়?
-   PHP তে implements কীভাবে কাজ করে?
-   
#####    OOP তে কম্পোজিশন এবং ইনহেরিটেন্সের মধ্যে পার্থক্য কী?
-   Inheritance: একটি ক্লাস অন্য ক্লাস থেকে বৈশিষ্ট্য ও আচরণ উত্তরাধিকারসূত্রে নেয়।
-   Composition: একটি ক্লাস অন্য একটি ক্লাসের অবজেক্টকে ধারণ করে এবং এটি তার বৈশিষ্ট্য ব্যবহার করে।

  
#####    মেথড ওভাররাইডিং কী এবং এটি কীভাবে করা হয়?
-   Method Overriding হল একটি সাবক্লাসে প্যারেন্ট ক্লাসের মেথড পুনঃসংজ্ঞায়িত করা।

  
#####    self কীওয়ার্ড কী এবং এটি কিভাবে ব্যবহৃত হয়?
-   self কীওয়ার্ড একটি ক্লাসের নিজের মেথড বা প্রপার্টি রেফার করার জন্য ব্যবহৃত হয়। উদাহরণ: self::$property

#####   PHP তে \_\_call() ম্যাজিক মেথড কীভাবে কাজ করে?
-   __call() ম্যাজিক মেথড একটি অবজেক্টের অজ্ঞাত মেথড কল করার সময় কল হয়।

  
#####    PHP তে **get() এবং **set() ম্যাজিক মেথডের কাজ কী?
-   __get($name): একটি অভ্যন্তরীণ প্রপার্টি অ্যাক্সেস করার জন্য ব্যবহৃত হয় যখন প্রপার্টি পাবলিক নয়।
-   __set($name, $value): একটি অভ্যন্তরীণ প্রপার্টি সেট করার জন্য ব্যবহৃত হয় যখন প্রপার্টি পাবলিক নয়।

  

### General Programming Concepts:

#####   ফাংশন এবং মেথডের মধ্যে পার্থক্য কী?
-   ফাংশন সাধারণত প্রোগ্রামের মূল অংশে ব্যবহৃত হয়, যেখানে মেথড একটি ক্লাসের অংশ এবং একটি অবজেক্টের আচরণ প্রদর্শন করে।

  
#####   কোড রিইউজেবলিটির গুরুত্ব কী?
-   কোড রিইউজেবিলিটি কোডের পুনঃব্যবহারযোগ্যতা বৃদ্ধি করে, ডুপ্লিকেট কোড কমায় এবং সফটওয়্যার রক্ষণাবেক্ষণ সহজ করে।


#####    কোড ডকুমেন্টেশন কিভাবে করা উচিত?
-   কোড ডকুমেন্টেশন মন্তব্য এবং ডকুমেন্টেশন স্ট্যান্ডার্ড ব্যবহার করে করা উচিত, যেমন PHPDoc, যা কোডের উদ্দেশ্য ও ব্যবহারের ব্যাখ্যা দেয়।

   
#####  ইউজার ইনপুট ভ্যালিডেশন কিভাবে করা হয়?
-   ইউজার ইনপুট ভ্যালিডেশন ফর্ম ডেটা যাচাই করে সঠিক ফর্ম্যাট এবং নিরাপত্তা নিশ্চিত করে, যেমন filter_var() ব্যবহার করে।

  
#####   এসকিউএল কোয়েরি অপটিমাইজেশন কীভাবে করা হয়?
-   ইনডেক্স ব্যবহার, কোয়েরি লেখা অপ্টিমাইজ করা, এবং ক্যাশিং কৌশল প্রয়োগ করে এসকিউএল কোয়েরি অপটিমাইজ করা হয়।


#####  প্রোগ্রামিং ডিজাইন প্যাটার্ন কী?
-   ডিজাইন প্যাটার্নগুলি কোডিং সমস্যা সমাধানে পুনরাবৃত্তিমূলক সমাধান প্রদান করে। কিছু সাধারণ ডিজাইন প্যাটার্ন হল সিঙ্গলটন, ফ্যাক্টরি, ডেকোরেটর।

  
#####  এক্সেপশন হ্যান্ডলিং কী এবং কেন এটি গুরুত্বপূর্ণ?
-   ক্সেপশন হ্যান্ডলিং এমন একটি প্রক্রিয়া যা ত্রুটি বা অসাধারণ পরিস্থিতি পরিচালনা করে। এটি প্রোগ্রামের ক্র্যাশ ঠেকায় এবং ডিফল্ট আচরণের পরিবর্তে বিশেষ কৌশল প্রয়োগ করে।

  
-   কোডিং স্ট্যান্ডার্ড এবং বেস্ট প্র্যাকটিস কী?
-   সফটওয়্যার ডেভেলপমেন্ট লাইফসাইকেল কী?






 
### Advanced Topics:

-   PHP তে নেমস্পেস কী এবং এটি কীভাবে ব্যবহৃত হয়?
-   
#####    PHP তে অটোলোডিং কীভাবে কাজ করে?
-   অটোলোডিং ক্লাস যখন প্রথমবার ব্যবহার করা হয় তখন স্বয়ংক্রিয়ভাবে ফাইল লোড করা হয়। spl_autoload_register() ফাংশন ব্যবহার করে এটি কনফিগার করা হয়।

 
#####   ক্যাশিং কৌশল এবং এটি কিভাবে কাজ করে?
-   ক্যাশিং ডেটা একটি দ্রুত প্রবাহিত স্টোরেজে সংরক্ষণ করে যাতে পরবর্তী অ্যাক্সেস দ্রুত হয়। উদাহরণস্বরূপ, অপারেশন ফলাফল ক্যাশে রাখা।

 
#####   RESTful API কী এবং এটি কিভাবে ব্যবহার করা হয়?
-  RESTful API একটি ওয়েব সার্ভিস যা HTTP প্রোটোকল ব্যবহার করে এবং CRUD অপারেশন (Create, Read, Update, Delete) সম্পাদন করতে সহায়তা করে।

   
#####  SOAP এবং REST এর মধ্যে পার্থক্য কী?
-   SOAP একটি প্রোটোকল যা XML ভিত্তিক এবং ভারী, REST হালকা এবং JSON/XML ভিত্তিক। REST সাধারণত HTTP প্রোটোকল ব্যবহার করে।

 
#####   PHP তে ইউনিট টেস্টিং কীভাবে করা হয়?
-   PHP তে ইউনিট টেস্টিং PHPUnit ফ্রেমওয়ার্ক ব্যবহার করে করা হয়। এটি কোডের একক


#####    আধুনিক PHP ফ্রেমওয়ার্কগুলোর তুলনা এবং বৈশিষ্ট্য কী?
-   কিছু জনপ্রিয় PHP ফ্রেমওয়ার্কের মধ্যে Laravel, Symfony, CodeIgniter, এবং Zend Framework অন্তর্ভুক্ত। Laravel সহজ ব্যবহারের জন্য টুলস এবং প্যাকেজ সরবরাহ করে, Symfony অত্যন্ত কনফিগারেবল এবং পুনঃব্যবহারযোগ্য কোড প্রদান করে, CodeIgniter দ্রুত ডেভেলপমেন্টের জন্য এবং Zend Framework বড় এবং জটিল অ্যাপ্লিকেশন নির্মাণের জন্য ব্যবহৃত হয়।

-   ভুয়া ডেটা তৈরির জন্য কোন টুলস ব্যবহার করা হয়?

### Debugging and Optimization:

-   কোড ডিবাগিং কৌশল কী কী?
#####  পিএইচপি কোডের পারফরম্যান্স কিভাবে উন্নত করা যায়?
-   পারফরম্যান্স উন্নত করার জন্য ইনডেক্স ব্যবহার, ক্যাশিং কৌশল প্রয়োগ, কো

   
#####     লগিং কিভাবে করা হয় এবং কোন টুলস ব্যবহার করা হয়?
-   লগিং বিভিন্ন স্তরের তথ্য রেকর্ড করার প্রক্রিয়া। PHP তে লগিং করার জন্য error_log(), Monolog, অথবা Log4PHP টুলস ব্যবহার করা যায়।

  
-   মেমরি লিক কী এবং এটি কিভাবে শনাক্ত করবেন?
-   এক্সেপশন ট্রেস কিভাবে বিশ্লেষণ করবেন?

### Database Related:

#####   SQL JOIN কি এবং এটি কত প্রকারের?
-   SQL JOIN একটি কৌশল যা একাধিক টেবিল থেকে ডেটা একত্রিত করে। প্রধান প্রকারগুলি হল INNER JOIN, LEFT JOIN, RIGHT JOIN, এবং FULL OUTER JOIN।

#####   ইনডেক্স কী এবং এটি ডেটাবেস পারফরম্যান্স কিভাবে প্রভাবিত করে?
   নডেক্স একটি ডেটাবেস অপটিমাইজেশন কৌশল যা দ্রুত অনুসন্ধান ও অর্ডারিংয়ের জন্য ব্যবহৃত হয়। এটি ডেটাবেসের পারফরম্যান্স বাড়ায়।


### Web Technologies:

#####   HTTP কী এবং HTTP Methods কী কী?
-   HTTP (Hypertext Transfer Protocol) একটি প্রোটোকল যা ক্লায়েন্ট এবং সার্ভারের মধ্যে ডেটা স্থানান্তর করে। HTTP Methods অন্তর্ভুক্ত: GET, POST, PUT, DELETE, PATCH, এবং HEAD.

  
#####   REST এবং GraphQL এর মধ্যে পার্থক্য কী?
-   REST একটি আর্কিটেকচারাল স্টাইল যা URI ব্যবহার করে রিসোর্স অ্যাক্সেস করে, GraphQL একটি কুয়েরি ভাষা যা ক্লায়েন্টকে নির্দিষ্ট তথ্য চাহিদা জানাতে সাহায্য করে এবং একক এন্ডপয়েন্ট ব্যবহার করে।

  
#####     AJAX কী এবং এটি কিভাবে কাজ করে?
-   AJAX (Asynchronous JavaScript and XML) একটি প্রযুক্তি যা ওয়েব পেজের নির্দিষ্ট অংশকে রি-লোড না করে ডাইনামিকভাবে আপডেট করতে সহায়তা করে। এটি XMLHttpRequest অবজেক্ট ব্যবহার করে কাজ করে।

  
#####     সার্ভার এবং ক্লায়েন্ট সাইডের মধ্যে পার্থক্য কী?
-   সার্ভার সাইড হল যেখানে সার্ভার কোড রান করে এবং ডেটা প্রসেসিং হয়, ক্লায়েন্ট সাইড হল যেখানে ইউজার ইন্টারফেস এবং ক্লায়েন্টের মধ্যে ডেটা প্রদর্শন হয়।

-   সেশন এবং কুকি ব্যবস্থাপনা কিভাবে করা হয়?


